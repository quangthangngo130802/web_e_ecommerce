# Thương Mại Điện Tử

Dự án base Vue 3 sử dụng Vite, Vue Router, Pinia, Axios, ESLint và Prettier.

## Cài đặt

```bash
npm install
npm run dev
```

Trên Windows PowerShell, dùng `npm.cmd` nếu máy đang chặn thực thi script:

```bash
npm.cmd install
npm.cmd run dev
```
